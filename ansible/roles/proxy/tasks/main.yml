---
- name: Launch httpd container
  docker_container:
    name: my-httpd-container
    image: greenway13/new-httpd:1.0
    networks:
      - name: front-network
    ports:
      - "80:80"
    restart_policy: unless-stopped
